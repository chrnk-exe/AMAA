import React, {FC} from 'react';
import { Box, Button } from '@mui/material';

const M1TestingPage: FC = () => {
	return (
		<Box display={'flex'} justifyContent={'center'} alignItems={'flex-start'}>
			<Button>TEST УРААА</Button>
			M1 Testing Page
		</Box>
	);
};

export default M1TestingPage;